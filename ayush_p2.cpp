#include <bits/stdc++.h>
using namespace std;
bool solve(vector<int> weights,int w){
    if(w<)

}
int main(){
    int sizes,weight;
    vector<int> weights;
    for(int i=0;i<sizes;i++){
        int temp;
        cin>>temp;
        weights.push_back(temp);
    }
    if(solve(weights,weight)){
        cout<<"true";
    }
    else{
        cout<<"false";
    }
}